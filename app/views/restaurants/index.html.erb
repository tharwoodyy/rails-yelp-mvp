<h1>All Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <h5><%= link_to restaurant.name, restaurant_path(restaurant.id) %></h5>
<% end %>
<i class="far fa-square"></i>
<%= link_to 'Create a new restaurant', new_restaurant_path %>
